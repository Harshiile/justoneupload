import { Children } from "@/app/(pages)/types/reactChildren";

export const metadata = {
  title: "Review - JustOneUpload",
};

export default function RootLayout({ children }: Children) {
  return children;
}
