import { LayoutProps } from "../../../../../../.next/types/app/layout";

export const metadata = {
  title: "Review - JustOneUpload",
};

export default function RootLayout({ children }: LayoutProps) {
  return children;
}
