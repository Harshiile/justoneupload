import { Children } from "../../types/reactChildren";

export const metadata = {
  title: "Upload - JustOneUpload",
};

export default function RootLayout({ children }: Children) {
  return children;
}
